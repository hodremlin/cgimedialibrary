<%= form_for(@video_player) do |f| %>
  <% if @video_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video_player.errors.count, "error") %> prohibited this video_player from being saved:</h2>

      <ul>
      <% @video_player.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :published_at %><br>
    <%= f.datetime_select :published_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
