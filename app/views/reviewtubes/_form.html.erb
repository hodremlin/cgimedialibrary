<%= form_for(@reviewtube) do |f| %>
  <% if @reviewtube.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reviewtube.errors.count, "error") %> prohibited this reviewtube from being saved:</h2>

      <ul>
      <% @reviewtube.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
