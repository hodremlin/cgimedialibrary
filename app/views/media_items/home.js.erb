$('#items').append('<%= j render partial: 'shared/media_items' %>');
<% if @items.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @items %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
