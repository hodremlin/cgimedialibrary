<%= render 'layouts/logo_header' %>
<div class="row">
  <% @logos.reverse.each do | logo | %>
    <%= render partial: 'shared/logo_item', locals: { logo: logo } %>
  <% end %>
</div>

<div id="infinite-scrolling">
  <%= will_paginate @logos %>
</div>